import SectionEditorWrapper from '@/Components/Feature/Backend/Cms/PageBuilder/SectionEditorWrapper';
import type { SectionEditorProps } from '@/Types/cms';

export default function Edit(props: SectionEditorProps) {
  return <SectionEditorWrapper {...props} />;
}
