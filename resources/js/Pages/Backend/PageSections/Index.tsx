import PageBuilderWrapper from '@/Components/Feature/Backend/Cms/PageBuilder/PageBuilderWrapper';
import type { PageBuilderProps } from '@/Types/cms';

export default function Index(props: PageBuilderProps) {
  return <PageBuilderWrapper {...props} />;
}
